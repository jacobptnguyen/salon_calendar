// Plain hex mirror of the CSS custom properties in src/index.css, for the
// few places (the PWA manifest in vite.config.js, <meta theme-color> in
// index.html) that need a literal value rather than a CSS variable. Keep
// these in sync.

export const theme = {
  bg: '#ffffff',
}
